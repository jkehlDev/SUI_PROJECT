<nav class="nav--bar">
    <% if(user.isSignIn){ %>
    <a class="nav--link--light nav--link--profil" title="Profil utilisateur" href="/user/profil"><%= user.name %></a>
    <% if(user.isAdmin){ %>
    <a class="nav--link--light" title="Page administrateur" href="/admin">Admin</a>
    <% } %>
    <a class="nav--link--light" title="Me déconnecter" href="/user/signout">Sign out</a>
    <% }else{ %>
    <a class="nav--link" title="Page de connexion" href="/signin">Sign in</a>
    <a class="nav--link" title="Page de création d'un compte utlisateur" href="/signup">Sign up</a>
    <% } %>
</nav>